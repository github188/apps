1. 打包程序
  sudo ./make-package.sh [--with-rootfs]

    --with-rootfs  指定是否需要把rootfs目录的内容打包

2. 安装程序到目标机器
  [sudo]./pkg-install.sh --package <filename> [--target IP]

    --package 指定安装的软件包路径和名称，由make-package.sh生成
    --target  指定安装的机器IP地址，默认是给本机安装，如果本机安装且当前用户为普通用户则需要指定sudo

